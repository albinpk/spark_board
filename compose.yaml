services:

  # api and frontend
  server:
    build: .
    restart: always
    ports:
      - "7070:3000"
    environment:
      POSTGRES_HOST: db

  # postgres database
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    volumes:
      - data:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: 1234

volumes:
  data:
